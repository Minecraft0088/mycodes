<html>
<html> teqi HTML sənədinin kökünü təmsil edir.
<head>
The <head> element is a container for metadata (data about data) and is placed between the <html> tag and the <body> tag.
<body>
The <body> tag defines the document's body.
<div>
The <div> tag defines a division  in an HTML document.
<h1>
The <h1> to <h6> tags are used to define HTML headings.
<p>
The <p> tag defines a paragraph.
<br>
The <br> tag inserts a single line break.
<hr>
The <hr> tag defines a thematic break in an HTML page (e.g. a shift of topic).
<a href>
The href attribute specifies the URL of the page the link goes to.
<img>
The <img> tag is used to embed an image in an HTML page.
<b>
The <b> tag specifies bold text without any extra importance.
<Attributes>
HTML attributes provide additional information about HTML elements.
<forms>
The HTML <form> element is used to create an HTML form for user input:
<HTML File Paths>
A file path specifies the location of a file inside a web folder structure. Its like an address of a file which helps the web browser to access the files. File paths are used to link external resources such as images, videos, style sheets, JavaScript, displaying other web pages etc.

2 ci der 

html tags
The HTML element is everything from the start tag to the end tag(her bir elementin baslangic ve sonunc taglari olur)
<ul>
The <ul> tag defines an unordered (bulleted) list.(listi siralamaq ucun)
<section>
The <section> tag defines a section in a document.
<meta>
The <meta> tag defines metadata about an HTML document. Metadata is data (information) about data.<meta> tags always go inside the <head> element, and are typically used to specify character set, page description, keywords, author of the document, and viewport settings.
<iframe>
An inline frame is used to embed another document within the current HTML document.
SEMANTIC
Many web sites contain HTML code like: <div id="nav"> <div class="header"> <div id="footer"> to indicate navigation, header, and footer.
In HTML there are some semantic elements that can be used to define different parts of a web page:  (sehifenin hisselerini mueyyen etmek ucun olan elementler.Semantic olamayan elementler (div ,span)sehifede her bir hisselerde istifade oluna biler) )
<article>
<aside>
<details>
<figcaption>
<figure>
<footer>
<header>
<main>
<mark>
<nav>
<section>
<summary>
<time>
Block and Inline elements
The P and the DIV elements are both block elements, and they will always start on a new line and take up the full width available(block elementlerin ozunun olculeri olur ve yeni setirden baslayirlar)
(Inline elementler yeni setirden baslamir ve setrin uzunlugu boyu yerlesir)





